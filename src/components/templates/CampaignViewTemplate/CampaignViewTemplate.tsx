import { FC } from 'react';
import { MainLayout } from '@organisms/layouts';

const CampaignViewTemplate: FC = () => {
  return <MainLayout>CampaignViewTemplate</MainLayout>;
};

export default CampaignViewTemplate;
